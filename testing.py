#ignore this, will be deleted eventually

import order

for x in range(0, 35):
        test = order.Order()
        print("Loop: " + str(x) + " | " + str(test.orderid) + " " + str(test.qty) + " " + str(test.timestamp) + " " + test.ecommercename)